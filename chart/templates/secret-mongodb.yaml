{{/*
Copyright (c) 2025 Fraunhofer Institute for Energy Economics and Energy System Technology (IEE)

This program and the accompanying materials are made available under the
terms of the Apache License, Version 2.0 which is available at
https://www.apache.org/licenses/LICENSE-2.0

SPDX-License-Identifier: Apache-2.0

Contributors:
     Fraunhofer IEE - initial API and implementation
*/}}

{{- if .Values.mongodb.auth.existingSecret }}
{{- $secret := lookup "v1" "Secret" (include "common.namespace" .) .Values.mongodb.auth.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.mongodb.auth.existingSecret }}
  namespace: {{ include "common.namespace" . | quote }}
type: Opaque
data:
  {{- if or (not $secret) (not $secret.data) (not (index $secret.data .Values.mongodb.auth.existingSecretPasswordKey)) }}
  {{ .Values.mongodb.auth.existingSecretPasswordKey }}: {{ randAlphaNum 20 | b64enc }}
  {{- else }}
  {{ .Values.mongodb.auth.existingSecretPasswordKey }}: {{ index $secret.data .Values.mongodb.auth.existingSecretPasswordKey }}
  {{- end }}
  {{ (index .Values.mongodb.customUsers 0).secretKeys.name }}: {{ (index .Values.mongodb.customUsers 0).name | b64enc }}
  {{ (index .Values.mongodb.customUsers 0).secretKeys.database }}: {{ (index .Values.mongodb.customUsers 0).database | b64enc }}
  {{- if or (not $secret) (not $secret.data) (not (index $secret.data (index .Values.mongodb.customUsers 0).secretKeys.password)) }}
  {{ (index .Values.mongodb.customUsers 0).secretKeys.password }}: {{ randAlphaNum 20 | b64enc }}
  {{- else }}
  {{ (index .Values.mongodb.customUsers 0).secretKeys.password }}: {{ index $secret.data (index .Values.mongodb.customUsers 0).secretKeys.password }}
  {{- end }}
{{- end }}